#!/bin/bash

set -e
set -x

image=$1; shift
prefix="dnephin"
tag="$(date +'%Y-%m-%d')"

if [ -z $image ]; then
    echo "Missing image name"
    exit -1
fi

sudo docker build -t "$prefix/$image:$tag" $@ $image
